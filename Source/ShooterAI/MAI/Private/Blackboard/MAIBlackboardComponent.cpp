//#include "Blackboard/MAIBlackboardComponent.h"
///#include "Blackboard/BlackboardKeyType_TMap.h"

/*template<typename keyname, typename keyvalue>
TMap<keyname, keyvalue> UMAIBlackboardComponent::GetValueAsTMap(const FName& KeyName) const
{
    return GetValue<TMap<keyname, keyvalue>>(KeyName);
}

template <typename keyname, typename keyvalue>
void UMAIBlackboardComponent::SetValueAsTMap(const FName& KeyName, TMap<keyname, keyvalue> TMapValue)
{
    const FBlackboard::FKey KeyID = GetKeyID(KeyName);
    SetValue<UBlackboardKeyType_TMap>(KeyID, TMapValue);
}*/
