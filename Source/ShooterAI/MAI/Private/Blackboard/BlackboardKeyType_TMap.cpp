//#include "Blackboard/BlackboardKeyType_TMap.h"
/*
template<typename keyname, typename keyvalue>
TMap<keyname, keyvalue> UBlackboardKeyType_TMap::GetValue(const UBlackboardKeyType_TMap* KeyObj, const uint8* RawData)
{
    return GetValueFromMemory<TMap<keyname, keyvalue>>(RawData);
}

template<typename keyname, typename keyvalue>
bool UBlackboardKeyType_TMap::SetValue(const UBlackboardKeyType_TMap* KeyObj, const uint8* RawData, TMap<keyname, keyvalue> Value)
{
    SetValueInMemory<TMap<keyname, keyvalue>>(RawData, Value);
}*/
